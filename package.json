{
  "name": "anchor-architectural-gate",
  "type": "module",
  "private": true,
  "description": "P0/P1 architectural-invariant gate prototype",
  "dependencies": {
    "@ast-grep/lang-go": "^0.0.6",
    "@ast-grep/lang-python": "^0.0.6",
    "@ast-grep/napi": "^0.43.0"
  },
  "scripts": {
    "test": "node run-tests.mjs",
    "gate": "node arch-gate.mjs",
    "arch": "node arch.mjs"
  },
  "bin": {
    "arch": "./arch.mjs"
  }
}
